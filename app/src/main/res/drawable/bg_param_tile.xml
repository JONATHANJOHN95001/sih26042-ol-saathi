<?xml version="1.0" encoding="utf-8"?>
<!--
  Selectable parameter tile: worksheet-type grid and activity-count row.

  Selected state carries the primary stroke and the secondaryContainer fill so
  the chosen tile reads as chosen at arm's length; unselected stays quiet.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/md_theme_secondaryContainer" />
            <stroke android:width="2dp" android:color="@color/md_theme_primary" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/md_theme_surfaceVariant" />
            <stroke android:width="1dp" android:color="@color/md_theme_outlineVariant" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</selector>
