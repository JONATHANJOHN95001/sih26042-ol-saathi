<?xml version="1.0" encoding="utf-8"?>
<!--
  The only screen a child ever looks at, after the "Show the class"
  design: one white card holding the picture, the target line as large as the
  space allows and the Hindi beneath it, then a single terracotta "Read aloud"
  and the speed chip. No header, no navigation, no provenance.

  The teacher turns the tablet around, so this has to read from across a
  classroom. Landscape (layout-land) puts the picture beside the words.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_showclass_canvas">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="32dp"
        android:paddingTop="72dp"
        android:paddingBottom="32dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@drawable/bg_showclass_card"
            android:orientation="vertical"
            android:clipToOutline="true">

            <!-- The picture. A pre-literate child reads this before any text. -->
            <ImageView
                android:id="@+id/imageCard"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="4"
                android:background="#F4EFE6"
                android:padding="24dp"
                android:scaleType="fitCenter"
                android:contentDescription="@null"
                android:visibility="gone" />

            <!-- Target line, the largest thing on screen. Auto-sized so a
                 one-word phrase and a full lesson sentence both fit. -->
            <TextView
                android:id="@+id/textTarget"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="4"
                android:layout_marginTop="16dp"
                android:paddingHorizontal="28dp"
                android:gravity="center"
                android:lineSpacingMultiplier="1.25"
                android:textColor="#0F3822"
                app:autoSizeTextType="uniform"
                app:autoSizeMinTextSize="28sp"
                app:autoSizeMaxTextSize="80sp"
                app:autoSizeStepGranularity="2sp" />

            <!-- Hindi underneath, smaller, in terracotta. -->
            <TextView
                android:id="@+id/textSource"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:paddingHorizontal="28dp"
                android:gravity="center"
                android:textSize="30sp"
                android:textStyle="bold"
                android:textColor="#B84310" />

            <!-- The pack's English gloss, when it has one. -->
            <TextView
                android:id="@+id/textGloss"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="24dp"
                android:paddingHorizontal="28dp"
                android:gravity="center"
                android:textSize="18sp"
                android:textColor="#57534E"
                android:visibility="gone" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnPlay"
                style="@style/ShowClass.ReadAloud"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/show_class_play"
                android:visibility="gone" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnSpeed"
                style="@style/ShowClass.Speed"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp" />
        </LinearLayout>
    </LinearLayout>

    <!-- Close, top right, out of the way. -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnClose"
        style="@style/ShowClass.Close"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="top|end"
        android:layout_margin="12dp"
        android:text="@string/show_class_close" />

</FrameLayout>
