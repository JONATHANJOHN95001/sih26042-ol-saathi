<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/md_theme_background">

    <!-- TopAppBar — Stitch "Earth & Script" style -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@color/md_theme_primary"
        app:titleTextColor="@color/md_theme_onPrimary"
        app:subtitleTextColor="@color/md_theme_primaryFixedDim"
        app:navigationIcon="@android:drawable/ic_menu_compass"
        app:navigationIconTint="@color/md_theme_onPrimary"
        app:title="TribalFLN Dashboard"
        app:subtitle="NIPUN FLN"
        app:menu="@menu/toolbar_menu" />

    <!-- Fragment Container — replaces NestedScrollView for multi-tab navigation -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Bottom Navigation Bar — Stitch design: 5 tabs with icon+label -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="@color/md_theme_surface"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:itemActiveIndicatorStyle="@style/Widget.Material3.BottomNavigationView.ActiveIndicator"
        app:elevation="0dp"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
