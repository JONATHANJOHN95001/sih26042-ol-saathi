<?xml version="1.0" encoding="utf-8"?>
<!-- One row of the home's "Continue teaching" card. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Home.ContinueRow"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <TextView
        android:id="@+id/textContIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        tools:text="📖" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textContTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#1C1917"
            tools:text="Neema dadi" />

        <TextView
            android:id="@+id/textContSub"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#78716C"
            tools:text="10 sentences · 3 questions" />
    </LinearLayout>

    <TextView
        android:id="@+id/btnContGo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_teach_translate"
        android:paddingHorizontal="14dp"
        android:paddingVertical="8dp"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        tools:text="Continue →" />
</LinearLayout>
